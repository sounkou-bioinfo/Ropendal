% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{byte-store}
\alias{byte-store}
\alias{byte_store}
\alias{store_cache}
\alias{store_block_cache}
\alias{store_cache_clear}
\alias{store_read}
\alias{store_read_aio}
\alias{store_write}
\alias{store_write_aio}
\alias{store_replace}
\alias{store_replace_aio}
\alias{store_exists}
\alias{store_exists_aio}
\alias{store_list}
\alias{store_list_aio}
\alias{store_delete}
\alias{store_delete_aio}
\title{Byte store adapter}
\usage{
byte_store(fs, prefix = "")

store_cache(
  store,
  cache_dir = tools::R_user_dir("Ropendal", "cache"),
  validate = c("last_modified_size", "none")
)

store_block_cache(
  store,
  cache_dir = tools::R_user_dir("Ropendal", "cache"),
  block_size = 8 * 1024^2,
  validate = c("last_modified_size", "none")
)

store_cache_clear(store)

store_read(
  store,
  key,
  mode = c("bytes", "raw"),
  offset = NULL,
  size = NULL,
  end = NULL,
  result = c("auto", "flat", "nested"),
  batch_concurrency = NULL,
  read_concurrency = NULL,
  chunk_size = NULL,
  coalesce_gap = NULL
)

store_read_aio(
  store,
  key,
  mode = c("bytes", "raw"),
  offset = NULL,
  size = NULL,
  end = NULL,
  result = c("auto", "flat", "nested"),
  batch_concurrency = NULL,
  read_concurrency = NULL,
  chunk_size = NULL,
  coalesce_gap = NULL
)

store_write(
  store,
  key,
  data,
  batch_concurrency = NULL,
  write_concurrency = NULL,
  chunk_size = NULL
)

store_write_aio(
  store,
  key,
  data,
  batch_concurrency = NULL,
  write_concurrency = NULL,
  chunk_size = NULL
)

store_replace(
  store,
  key,
  data,
  batch_concurrency = NULL,
  write_concurrency = NULL,
  chunk_size = NULL
)

store_replace_aio(
  store,
  key,
  data,
  batch_concurrency = NULL,
  write_concurrency = NULL,
  chunk_size = NULL
)

store_exists(store, key, batch_concurrency = NULL)

store_exists_aio(store, key, batch_concurrency = NULL)

store_list(
  store,
  path = "",
  recursive = FALSE,
  limit = NULL,
  start_after = NULL
)

store_list_aio(
  store,
  path = "",
  recursive = FALSE,
  limit = NULL,
  start_after = NULL
)

store_delete(store, key, recursive = FALSE, batch_concurrency = NULL)

store_delete_aio(store, key, recursive = FALSE, batch_concurrency = NULL)
}
\arguments{
\item{fs}{Ropendal filesystem handle.}

\item{prefix}{Root-relative filesystem prefix used as the store root.}

\item{store}{Byte store object returned by \code{byte_store()}, \code{store_cache()},
or \code{store_block_cache()}.}

\item{cache_dir}{Local directory used by \code{store_cache()} or
\code{store_block_cache()}.}

\item{validate}{Cache validation strategy. \code{"last_modified_size"} compares
parent object size and modification time before using a cached value.
\code{"none"} skips modification-time checks; block caches still stat the
parent to bound ranges and refresh when cached block metadata has a
different object size.}

\item{block_size}{Fixed block size in bytes for \code{store_block_cache()}.}

\item{key}{Store-relative object key or keys.}

\item{mode}{Read materialization: \code{OpendalBytes} handles or raw vectors.}

\item{offset, size, end}{Optional byte-range controls forwarded to the read
operation.}

\item{result}{Requested read result shape.}

\item{batch_concurrency}{Optional maximum number of keys to process
concurrently.}

\item{read_concurrency, write_concurrency}{Optional per-object OpenDAL transfer
concurrency.}

\item{chunk_size}{Optional transfer chunk size in bytes.}

\item{coalesce_gap}{Optional byte gap for coalescing nearby read ranges.}

\item{data}{Raw vector, \code{OpendalBytes}, or list of raw vectors/byte handles.}

\item{path}{Store-relative directory path for \code{store_list()}.}

\item{recursive}{Whether to list or delete recursively.}

\item{limit}{Optional maximum listing entries.}

\item{start_after}{Optional store-relative listing continuation marker.}
}
\value{
\code{byte_store()}, \code{store_cache()}, and \code{store_block_cache()} return
byte-store objects. Store operations return the corresponding filesystem values with paths rewritten
relative to the store root for listings.
}
\description{
A small key-to-bytes adapter over an \code{OpendalFs} prefix. It is intended as a
substrate for Zarr-like chunk layouts and other index-driven readers that want
store-relative keys without leaving Ropendal's byte-first filesystem API.
}
\details{
\code{store_read()} returns \code{OpendalBytes} by default so callers can stay below R
raw-vector materialization until they explicitly ask for \code{mode = "raw"} or
\code{as.raw()}. The store layer is intentionally byte-only: serializers,
deserializers, codecs, and format parsers belong above it.

\code{store_cache()} wraps a byte store in an explicit local full-object cache. It
is useful for chunked layouts where each key is already a small block. Partial
byte-range reads and non-default read shaping bypass this first cache layer.
\code{store_block_cache()} wraps a byte store in an explicit fixed-size block cache
for scalar complete or range reads with \code{result = "auto"} while keeping
format semantics above the store layer. Vectorized ranges, vectorized keys,
and non-auto result shapes bypass this cache adapter and read from the parent
store directly.
}
\examples{
root <- tempfile("ropendal-store-")
dir.create(root)
fs <- opendal("fs", root = root)
store <- byte_store(fs, "array.zarr")
store_write(store, "zarr.json", charToRaw("{}"))
as.raw(store_read(store, "zarr.json"))
cached <- store_cache(store, tempfile("ropendal-cache-"), validate = "none")
as.raw(store_read(cached, "zarr.json"))
}
