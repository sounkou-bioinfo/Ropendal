% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{Ropendal-api}
\alias{Ropendal-api}
\alias{CredentialProvider}
\alias{OpendalBytes}
\alias{opendal}
\alias{opendal_uri}
\alias{credentials_s3}
\alias{credentials_gdrive}
\alias{credentials_gdrive3}
\alias{credential_schemes}
\alias{credential_config}
\alias{credential_summary}
\alias{opt}
\alias{opt<-}
\alias{serial_config}
\alias{codec_config}
\alias{serialize_raw}
\alias{deserialize_raw}
\alias{fs_info}
\alias{fs_capabilities}
\alias{fs_normalize_path}
\alias{fs_read}
\alias{fs_read_aio}
\alias{fs_read_bytes}
\alias{fs_read_bytes_aio}
\alias{as.raw.OpendalBytes}
\alias{length.OpendalBytes}
\alias{fs_read_iter}
\alias{read_iter_next}
\alias{read_iter_collect}
\alias{fs_ls_iter}
\alias{fs_walk_iter}
\alias{ls_iter_next}
\alias{ls_iter_collect}
\alias{walk_iter_next}
\alias{walk_iter_collect}
\alias{fs_seek}
\alias{fs_tell}
\alias{fs_write}
\alias{fs_write_aio}
\alias{fs_replace}
\alias{fs_replace_aio}
\alias{fs_append}
\alias{fs_append_aio}
\alias{fs_write_iter}
\alias{write_iter_write}
\alias{write_iter_close}
\alias{fs_stat}
\alias{fs_stat_aio}
\alias{fs_stats}
\alias{fs_stats_aio}
\alias{fs_exists}
\alias{fs_exists_aio}
\alias{fs_ls}
\alias{fs_ls_aio}
\alias{fs_mkdir}
\alias{fs_mkdir_aio}
\alias{fs_delete}
\alias{fs_delete_aio}
\alias{fs_copy}
\alias{fs_copy_aio}
\alias{fs_rename}
\alias{fs_rename_aio}
\alias{collect_aio}
\alias{collect_aio_}
\alias{call_aio}
\alias{call_aio_}
\alias{stop_aio}
\alias{poll_aio}
\alias{cv}
\alias{cv_value}
\alias{cv_reset}
\alias{cv_signal}
\alias{cv_wait}
\alias{cv_until}
\alias{aio_monitor}
\alias{read_monitor}
\alias{race_aio}
\alias{unresolved}
\alias{is_error_value}
\alias{error_kind}
\alias{error_message}
\alias{error_operation}
\alias{error_path}
\title{Ropendal filesystem API}
\usage{
opendal(scheme = "fs", ..., root = NULL, config = list(), auth = NULL,
        headers = NULL)
opendal_uri(uri, headers = NULL)
credentials_s3(access_key_id, secret_access_key,
               session_token = "", region = "", source = "direct")
credentials_gdrive(access_token = "", refresh_token = "",
                   client_id = "", client_secret = "", source = "direct")
credentials_gdrive3(secret_json,
                    tokens_json = file.path(dirname(secret_json), "tokens.json"),
                    scope = "https://www.googleapis.com/auth/drive")
credential_schemes(provider)
credential_config(provider, service)
credential_summary(provider)
fs_info(fs)
fs_capabilities(fs)
fs_normalize_path(fs, path, directory = FALSE)
opt(fs, name)
opt(fs, name) <- value
serial_config(class, sfunc, ufunc)
codec_config(name, class = "raw", sfunc = NULL, ufunc = NULL)
serialize_raw(x, config = list())
deserialize_raw(x, config = list())
fs_read(fs, path, offset = 0, size = NULL, end = NULL,
        result = c("auto", "flat", "nested"), batch_concurrency = NULL,
        read_concurrency = NULL, chunk_size = NULL, coalesce_gap = NULL,
        mode = c("raw", "serial", "codec"),
        serial_config = opt(fs, "serial"), codec = opt(fs, "codec"))
fs_read_aio(fs, path, offset = 0, size = NULL, end = NULL,
            result = c("auto", "flat", "nested"), batch_concurrency = NULL,
            read_concurrency = NULL, chunk_size = NULL, coalesce_gap = NULL,
            mode = c("raw", "serial", "codec"),
            serial_config = opt(fs, "serial"), codec = opt(fs, "codec"))
fs_read_bytes(fs, path, offset = 0, size = NULL, end = NULL,
              result = c("auto", "flat", "nested"), batch_concurrency = NULL,
              read_concurrency = NULL, chunk_size = NULL, coalesce_gap = NULL)
fs_read_bytes_aio(fs, path, offset = 0, size = NULL, end = NULL,
                  result = c("auto", "flat", "nested"), batch_concurrency = NULL,
                  read_concurrency = NULL, chunk_size = NULL, coalesce_gap = NULL)
\method{as.raw}{OpendalBytes}(x)
\method{length}{OpendalBytes}(x)
fs_read_iter(fs, path, chunk_size, offset = 0, size = NULL,
             read_concurrency = NULL, coalesce_gap = NULL)
read_iter_next(iter)
read_iter_collect(iter)
fs_tell(iter)
fs_seek(iter, offset, whence = c("start", "current", "end"))
fs_write(fs, path, data, batch_concurrency = NULL,
         write_concurrency = NULL, chunk_size = NULL,
         mode = c("raw", "serial", "codec"),
         serial_config = opt(fs, "serial"), codec = opt(fs, "codec"))
fs_write_aio(fs, path, data, batch_concurrency = NULL,
             write_concurrency = NULL, chunk_size = NULL,
             mode = c("raw", "serial", "codec"),
             serial_config = opt(fs, "serial"), codec = opt(fs, "codec"))
fs_replace(fs, path, data, batch_concurrency = NULL,
           write_concurrency = NULL, chunk_size = NULL,
           mode = c("raw", "serial", "codec"),
           serial_config = opt(fs, "serial"), codec = opt(fs, "codec"))
fs_replace_aio(fs, path, data, batch_concurrency = NULL,
               write_concurrency = NULL, chunk_size = NULL,
               mode = c("raw", "serial", "codec"),
               serial_config = opt(fs, "serial"), codec = opt(fs, "codec"))
fs_append(fs, path, data, batch_concurrency = NULL,
          write_concurrency = NULL, chunk_size = NULL,
          mode = c("raw", "serial", "codec"),
          serial_config = opt(fs, "serial"), codec = opt(fs, "codec"))
fs_append_aio(fs, path, data, batch_concurrency = NULL,
              write_concurrency = NULL, chunk_size = NULL,
              mode = c("raw", "serial", "codec"),
              serial_config = opt(fs, "serial"), codec = opt(fs, "codec"))
fs_write_iter(fs, path, create = TRUE, append = FALSE,
              write_concurrency = NULL, chunk_size = NULL)
write_iter_write(iter, data)
write_iter_close(iter)
fs_stat(fs, path, batch_concurrency = NULL)
fs_stat_aio(fs, path, batch_concurrency = NULL)
fs_stats(fs, path, batch_concurrency = NULL)
fs_stats_aio(fs, path, batch_concurrency = NULL)
fs_exists(fs, path, batch_concurrency = NULL)
fs_exists_aio(fs, path, batch_concurrency = NULL)
fs_ls(fs, path = "", recursive = FALSE)
fs_ls_aio(fs, path = "", recursive = FALSE)
fs_ls_iter(fs, path = "", recursive = FALSE, page_size = 1000)
fs_walk_iter(fs, path = "", page_size = 1000)
ls_iter_next(iter)
ls_iter_collect(iter)
walk_iter_next(iter)
walk_iter_collect(iter)
fs_mkdir(fs, path)
fs_mkdir_aio(fs, path, batch_concurrency = NULL)
fs_delete(fs, path, recursive = FALSE, batch_concurrency = NULL)
fs_delete_aio(fs, path, recursive = FALSE, batch_concurrency = NULL)
fs_copy(fs, from, to)
fs_copy_aio(fs, from, to, batch_concurrency = NULL)
fs_rename(fs, from, to)
fs_rename_aio(fs, from, to, batch_concurrency = NULL)
collect_aio(aio)
collect_aio_(aio)
call_aio(aio)
call_aio_(aio)
stop_aio(aio)
poll_aio(aio)
cv()
cv_value(cv)
cv_reset(cv)
cv_signal(cv)
cv_wait(cv, interval = 0.001)
cv_until(cv, msec = 0, interval = 0.001)
aio_monitor(aio, cv = cv())
read_monitor(monitor)
race_aio(aio, timeout = NULL, interval = 0.001)
unresolved(x = NULL)
is_error_value(x)
error_kind(x)
error_message(x)
error_operation(x)
error_path(x)
}
\arguments{
\item{scheme}{OpenDAL service scheme.}

\item{...}{Named service configuration entries.}

\item{root}{Root path/prefix for the service.}

\item{config}{For \code{opendal()}, named list of service configuration entries.
For \code{serialize_raw()} / \code{deserialize_raw()}, serialization config.}

\item{auth}{Explicit credential provider.}

\item{headers}{Named scalar character list/vector of HTTP headers for
\code{http}/\code{https} filesystems; values may contain credentials and are not
printed by Ropendal.}

\item{uri}{OpenDAL URI.}

\item{access_key_id, secret_access_key}{S3-compatible access key fields.}

\item{session_token}{Optional S3-compatible session token.}

\item{region}{Optional S3-compatible signing region.}

\item{access_token}{Google Drive access token, mutually exclusive with
\code{refresh_token}.}

\item{refresh_token}{Google Drive refresh token.}

\item{client_id, client_secret}{OAuth client fields required with
\code{refresh_token}.}

\item{source}{Redacted source label for a credential provider.}

\item{secret_json}{Path to a JSON file containing Google Drive \code{client_id}
and \code{client_secret}.}

\item{tokens_json}{Path to a token JSON file containing a refresh token.}

\item{scope}{Scope used to choose a refresh token from \code{tokens_json}.}

\item{provider}{Credential provider object.}

\item{service}{OpenDAL service scheme for credential materialization.}

\item{fs}{Ropendal filesystem handle.}

\item{path}{Root-relative path or paths.}

\item{directory}{Whether to normalize as a directory path.}

\item{offset}{Zero-based byte offset.}

\item{size}{Number of bytes to read, or \code{NULL} to read to EOF.}

\item{end}{Exclusive byte end offset.}

\item{result}{Requested result shape.}

\item{name}{Option name.}

\item{value}{Option value.}

\item{class}{Class name or names matched for custom serialization.}

\item{sfunc, ufunc}{Serializer and deserializer functions for
\code{serial_config()}. R-closure codecs are not supported; codec transforms
are native byte transforms.}

\item{mode}{Materialization mode: raw bytes, serialized R objects, or raw
bytes passed through an explicit codec.}

\item{serial_config}{Serialization config, normally \code{opt(fs, "serial")}.}

\item{codec}{Optional native byte codec name or \code{codec_config()} object,
normally \code{opt(fs, "codec")}.}

\item{batch_concurrency}{Optional maximum number of independent paths/ranges
to process concurrently.}

\item{read_concurrency}{Optional per-object OpenDAL read concurrency for large
reads where the backend supports chunked/concurrent reads.}

\item{write_concurrency}{Optional per-object OpenDAL write concurrency for
large writes where the backend supports multipart/concurrent writes.}

\item{chunk_size}{Optional read/write chunk size in bytes for OpenDAL's
per-object transfer planning.}

\item{coalesce_gap}{Optional byte gap for coalescing nearby read ranges.}

\item{data}{Raw vector, \code{OpendalBytes} handle, or list mixing raw vectors
and \code{OpendalBytes} handles for multiple paths in raw mode; arbitrary R
object or list of objects in serial mode.}

\item{iter}{Read, write, listing, or walking iterator handle.}

\item{whence}{Seek origin for read iterators: iterator \code{start}, \code{current}, or \code{end}.}

\item{create}{Whether a write iterator should create only and fail if the target exists.}

\item{append}{Whether a write iterator should append rather than replace.}

\item{recursive}{Whether to recurse for operations that support it.}

\item{page_size}{Maximum number of entries returned by one iterator page.}

\item{from, to}{Source and destination paths.}

\item{aio}{Aio handle.}

\item{cv}{Condition variable object from \code{cv()}.}

\item{monitor}{Monitor handle from \code{aio_monitor()}.}

\item{msec}{Milliseconds to wait for \code{cv_until()}.}

\item{timeout}{Optional milliseconds to wait for \code{race_aio()}.}

\item{interval}{Poll interval in seconds for R-thread wait helpers.}

\item{x}{Object to inspect.}
}
\value{
Filesystem handles, raw vectors, \code{OpendalBytes} handles,
deserialized R objects, metadata lists, logical results, Aio handles, or
classed error values depending on the operation and mode.
}
\description{
Byte-oriented filesystem operations backed by Apache OpenDAL. The current
implementation includes local \code{fs}, HTTP, S3-compatible, and Google Drive
handles; raw byte operations; metadata and listing where supported; error
values; read Aio handles; and a pure C API.
}
\details{
Filesystem failures are returned as classed error values. Invalid arguments
and internal runtime failures may signal R errors. Credential helpers return
classed providers with redacted printing; pass them with \verb{auth =}.

Aio handles expose read-only active bindings. \verb{$value} returns an
\code{unresolvedValue} while the operation is pending and the resolved value or
error value afterwards. \verb{$data} and \verb{$result} are aliases for \verb{$value}, while
\verb{$state}, \verb{$resolved}, and \verb{$error} expose readiness and error inspection.
\code{collect_aio()} waits and returns the value. \code{call_aio()} waits, updates the
Aio, and returns the Aio invisibly. \code{unresolved()} constructs the unresolved
sentinel; \code{unresolved(aio)} and \code{unresolved(value)} are predicates.
}
